rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    match /mooieman/{imageId} {
       allow read;
       allow write: if request.resource.contentType.matches('image/.*')
                    && request.resource.contentType == resource.contentType
                    && request.auth.uid != null
    }
    match /mooieman/original/{imageId} {
       allow read;
       allow write: if request.resource.contentType.matches('image/.*')
                    && request.resource.contentType == resource.contentType
                    && request.auth.uid != null
    }
     match /fraaievrouw/{imageId} {
       allow read;
       allow write: if request.resource.contentType.matches('image/.*')
                    && request.resource.contentType == resource.contentType
                    && request.auth.uid != null
    }
    match /fraaievrouw/original/{imageId} {
       allow read;
       allow write: if request.resource.contentType.matches('image/.*')
                    && request.resource.contentType == resource.contentType
                    && request.auth.uid != null
    }
  }
}